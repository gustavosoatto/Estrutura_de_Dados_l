#include "list.h"
#include <iostream>
using namespace std;

int main()
{
    List lista;
    for (int i = 0; i < 10; i++)
        lista.Insert(i, i + 1);

    for (int i = lista.Size(); i > 0; i--)
    {
        lista.Insert(i, )
    }
}